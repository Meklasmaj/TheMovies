<Application x:Class="TheMovies.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TheMovies.UI"
             xmlns:viewModels="clr-namespace:TheMovies.UI.ViewModels">
    <Application.Resources>
        <!-- Creates a connection between ViewModel and View -->
        <DataTemplate DataType="{x:Type viewModels:MainViewViewModel}">
             <local:MainView/>
         </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:RegisterMovieViewModel}">
             <local:RegisterMovieView/>
         </DataTemplate>
    </Application.Resources>
</Application>
